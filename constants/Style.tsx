import {StyleSheet} from "react-native";

const MyStyle = StyleSheet.create({
    textTest : {
        color: 'yellow',
        fontSize: 20,
        fontWeight: "bold",
        marginTop: 20,
    }
})
export default MyStyle;